<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Edit Users</title>
        <link rel="stylesheet" href="/stylesheets/style.css">
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
            integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
            crossorigin="anonymous">
    </head>
    <body class="bg-dark text-white">
        <div class="pt-4 px-4">

            <ul class="nav mb-4">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page"
                        href="/">Back Home</a>
                </li>
                <li>
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page"
                            href="/read">Read Users</a>
                    </li>
                </li>
            </ul>

            <h1 class="mb-3">Edit User Details</h1>
            <div class="mt-3 p-3">
                <form action="/update/<%= user._id %>" method="post">
                    <input type="text"
                        class="mx-2 px-3 py-2 bg-dark border-info text-white"
                        value="<%= user.name %>" name="name">
                    <input type="text"
                        class="mx-2 my-2 px-3 py-2 bg-dark border-info text-white"
                        value="<%= user.email%>" name="email">
                    <input type="text"
                        class="mx-2 my-2 px-3 py-2 bg-dark border-info text-white"
                        name="imageurl"
                        value="<%= user.imageurl %>">
                    <input type="submit"
                        class="mx-2 my-2 px-2 py-2 border-info text-white submit-button"
                        value="Update User">
                </form>
            </div>
        </div>
    </body>
</html>